<VirtualHost *:443>
    ServerName token.example.com

    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/token.example.com/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/token.example.com/privkey.pem

    ProxyPreserveHost On
    RequestHeader unset X-Remote-User

    <Location /token>
        Require ip 127.0.0.1
        Require ip 10.0.0.0/8
        Require ip 172.16.0.0/12
        Require ip 192.168.0.0/16
        ProxyPass http://127.0.0.1:8091/token
        ProxyPassReverse http://127.0.0.1:8091/token
    </Location>

    ErrorLog ${APACHE_LOG_DIR}/ejabberd-token-service-python-error.log
    CustomLog ${APACHE_LOG_DIR}/ejabberd-token-service-python-access.log combined
</VirtualHost>
